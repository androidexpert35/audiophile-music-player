package com.androidexpert35.audiophilemusicplayer.presentation.viewmodel.settings

/**
 * One-shot effects emitted from the Library Sections settings ViewModel.
 *
 * Visibility and order changes are applied optimistically and persisted silently in
 * the background, so there is currently nothing for this screen to signal through a
 * one-shot effect.
 */
sealed interface LibrarySectionsSettingsUiEffect
