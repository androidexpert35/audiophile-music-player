package com.androidexpert35.audiophilemusicplayer.domain.model.playback

/**
 * Shuffle behaviour for the playback queue.
 */
enum class ShuffleMode {
    /** Tracks play in their original queue order. */
    OFF,
    /** Tracks play in a randomised order. */
    ON
}

