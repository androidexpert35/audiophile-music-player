package com.androidexpert35.audiophilemusicplayer.data.repository

import androidx.core.content.FileProvider

/** Grants the selected email app temporary read access to private diagnostic attachments. */
class BugReportFileProvider : FileProvider()
